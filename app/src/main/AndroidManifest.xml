<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ye.futurecarefree">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

<!--    默认是没有Actionbar的标题栏-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_51job"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".ui.activity.HomeSearchActivity"
            android:theme="@style/HomeTheme"/>
        <activity android:name=".ui.activity.HomeChoseLocationActivity"
            android:theme="@style/NoActionBar"/>
        <activity
            android:name=".ui.activity.DemandActivity"
            android:launchMode="singleTop"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".ui.activity.HomeActivity"
            android:launchMode="singleTop"
            android:theme="@style/HomeTheme" />
        <activity
            android:name=".ui.activity.LaunchHelpActivity"
            android:launchMode="singleTop"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".ui.activity.LaunchActivity"
            android:theme="@style/NoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>